/**
 * JSON Web Token (JWT) module.
 * <p>
 * Dedicated to the lifecycle of JWTs, including generation, signature validation,
 * token extraction from requests, and the JWT authentication filter chain.
 * This module is intended to be self-contained so it can be conditionally
 * loaded in a starter environment.
 * </p>
 */
package com.dollop.app.jwt;
